export interface Destination {
  id: string;
  title: string;
  subtitle: string;
  tagline: string;
  description: string;
  detail: string;
  emoji: string;
  color: string;
}

export interface ProgrammeCard {
  icon: string;
  title: string;
  desc: string;
  color: string;
}
